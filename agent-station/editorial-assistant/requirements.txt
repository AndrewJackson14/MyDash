# Editorial Assistant — Python deps.
#
# Phase B/C: FastAPI server + voice profile resolver + four skills
# that call Gemini via agent-station/shared/gemini.py (which uses
# `requests` directly — no SDK needed). If we later move to
# google-generativeai, add it here.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
requests>=2.31
python-dotenv>=1.0
