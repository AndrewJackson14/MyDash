// Login — Phase C placeholder. Real implementation per spec §5.1
// (magic link primary, password collapsed-by-default).
import Placeholder from "./Placeholder";
export default function Login() {
  return <Placeholder title="Sign in" body="Login flow lands in Phase C." />;
}
