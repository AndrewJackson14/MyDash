// /setup — Phase C placeholder. Real implementation per spec §5.2
// (request_portal_setup_link entry).
import Placeholder from "./Placeholder";
export default function Setup() {
  return <Placeholder title="Set up your account" body="Setup flow lands in Phase C." />;
}
